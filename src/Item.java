import java.awt.Color;
import java.awt.Graphics;
import java.util.Random;

public class Item { 
	
	
	/**
	 * Extension Challenge : 
	 * 
	 * Implement a scoring system within the game: 
	 * Each time the ball hits the floor score is reduced. 
	 * Items will float on top of some platforms, when collected (by collision with ball) 
	 * this will increase the score. 
	 * 
	 * Try adding a background image such as a city skyline (can make one in 5 mins in ms paint.)
	 */
	
	public void update(StartingPoint sp, Ball b) {
	}

	private void checkForCollision(Ball b) {
	}

	public void paint(Graphics g) {
	}

}
